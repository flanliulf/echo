{
  "name": "echo",
  "version": "1.0.0",
  "description": "Echo服务，提供返回hello的接口",
  "main": "src/index.js",
  "engines": {
    "node": "20.19.0"
  },
  "scripts": {
    "start": "pm2 start ecosystem.config.js --env production",
    "dev": "nodemon src/index.js",
    "deploy": "bash deploy/deploy.sh"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pm2": "^5.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
